import React from 'react'
import LoginPage from './loginClient'

export const metadata = {
  title: `Login to portal`,
};

const page = () => {
  return (
    <LoginPage />
  )
}

export default page
