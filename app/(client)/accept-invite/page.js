import React from 'react'
import ResetPasswordPage from './resetClient'

export const metadata = {
  title: `Accept Invite`,
};

const page = () => {
  return (
    <ResetPasswordPage />
  )
}

export default page
