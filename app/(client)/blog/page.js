import React from 'react'
import Blog from './blogClient'

export const metadata = {
  title: "Explore Our Blog",
}

const page = () => {
  return (
    <Blog />
  )
}

export default page
